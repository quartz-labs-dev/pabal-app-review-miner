# Agent Guide

Canonical agent instructions live in [`AGENTS.md`](./AGENTS.md).
When HTML generation logic in `src/renderReportHtml.ts` changes, run `npm run report:render-html -- --all` to regenerate all app reports.
